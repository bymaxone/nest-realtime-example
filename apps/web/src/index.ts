/**
 * @fileoverview Entry point placeholder for the nest-realtime-example web app.
 * @layer utility
 *
 * Replaced by the Next.js App Router skeleton once application code lands.
 * Kept as a real, type-checked module so the workspace's typecheck and
 * build gates are truthful from the first commit, rather than passing on
 * empty input.
 */

/** Identifies this package in workspace-wide tooling output. */
export const WEB_PACKAGE_NAME = '@nest-realtime-example/web' as const;
