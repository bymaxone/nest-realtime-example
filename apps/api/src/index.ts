/**
 * @fileoverview Entry point placeholder for the nest-realtime-example API.
 * @layer utility
 *
 * Replaced by the NestJS bootstrap once application code lands. Kept as a
 * real, type-checked module so the workspace's typecheck and build gates
 * are truthful from the first commit, rather than passing on empty input.
 */

/** Identifies this package in workspace-wide tooling output. */
export const API_PACKAGE_NAME = '@nest-realtime-example/api' as const;
