/**
 * @fileoverview Commit message linting rules for the nest-realtime-example workspace.
 * @layer config
 *
 * Extends the community Conventional Commits preset unchanged; the repository's
 * own type/scope vocabulary is documented in .gitmessage for contributors.
 */

module.exports = {
  extends: ['@commitlint/config-conventional'],
};
