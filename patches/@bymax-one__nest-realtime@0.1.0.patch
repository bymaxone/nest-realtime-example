diff --git a/dist/react/index.cjs b/dist/react/index.cjs
index ed715d9..1a7e296 100644
--- a/dist/react/index.cjs
+++ b/dist/react/index.cjs
@@ -28,8 +28,10 @@ function useRealtimeSse(opts) {
   const [events, setEvents] = react.useState([]);
   const [lastEvent, setLastEvent] = react.useState(void 0);
   const [error, setError] = react.useState(void 0);
+  const [reconnectAttempts, setReconnectAttempts] = react.useState(0);
   const sourceRef = react.useRef(null);
   const reconnectMsRef = react.useRef(opts.reconnectInitialMs ?? 1e3);
+  const attemptsRef = react.useRef(0);
   const timerRef = react.useRef(null);
   const connect = react.useCallback(() => {
     if (opts.enabled === false) return;
@@ -44,10 +46,15 @@ function useRealtimeSse(opts) {
       setConnected(true);
       setError(void 0);
       reconnectMsRef.current = opts.reconnectInitialMs ?? 1e3;
+      attemptsRef.current = 0;
+      setReconnectAttempts(0);
     };
     source.onerror = () => {
       setConnected(false);
       setError(new Error("SSE connection error"));
+      attemptsRef.current += 1;
+      setReconnectAttempts(attemptsRef.current);
+      if (opts.maxAttempts !== void 0 && attemptsRef.current >= opts.maxAttempts) return;
       const delay = Math.min(reconnectMsRef.current * 2, opts.reconnectMaxMs ?? 3e4);
       reconnectMsRef.current = delay;
       timerRef.current = setTimeout(connect, delay);
@@ -65,7 +72,12 @@ function useRealtimeSse(opts) {
     for (const name of NAMED_SSE_EVENTS) {
       source.addEventListener(name, handleEvent);
     }
-  }, [opts.enabled, opts.url, opts.withCredentials, opts.reconnectInitialMs, opts.reconnectMaxMs]);
+    if (opts.events) {
+      for (const name of opts.events) {
+        source.addEventListener(name, handleEvent);
+      }
+    }
+  }, [opts.enabled, opts.url, opts.withCredentials, opts.reconnectInitialMs, opts.reconnectMaxMs, opts.maxAttempts, opts.events]);
   react.useEffect(() => {
     if (opts.enabled === false) return;
     connect();
@@ -80,9 +92,11 @@ function useRealtimeSse(opts) {
   }, [connect, opts.enabled]);
   const reconnect = react.useCallback(() => {
     reconnectMsRef.current = opts.reconnectInitialMs ?? 1e3;
+    attemptsRef.current = 0;
+    setReconnectAttempts(0);
     connect();
   }, [connect, opts.reconnectInitialMs]);
-  return { connected, events, lastEvent, error, reconnect };
+  return { connected, events, lastEvent, error, reconnect, reconnectAttempts };
 }
 var MAX_EVENTS2 = 100;
 function useRealtimeWs(opts) {
@@ -155,6 +169,10 @@ function useRealtime(opts) {
   const sseResult = useRealtimeSse({
     url: opts.url,
     ...opts.withCredentials !== void 0 ? { withCredentials: opts.withCredentials } : {},
+    ...opts.events !== void 0 ? { events: opts.events } : {},
+    ...opts.reconnectInitialMs !== void 0 ? { reconnectInitialMs: opts.reconnectInitialMs } : {},
+    ...opts.reconnectMaxMs !== void 0 ? { reconnectMaxMs: opts.reconnectMaxMs } : {},
+    ...opts.maxAttempts !== void 0 ? { maxAttempts: opts.maxAttempts } : {},
     enabled: !isWs
   });
   const wsResult = useRealtimeWs({
@@ -164,7 +182,7 @@ function useRealtime(opts) {
     enabled: isWs
   });
   if (isWs) {
-    return { transport: "websocket", ...wsResult };
+    return { transport: "websocket", reconnectAttempts: 0, ...wsResult };
   }
   return {
     transport: "sse",
@@ -176,8 +194,8 @@ function useRealtime(opts) {
 
 // src/react/hooks/use-realtime-connection.ts
 function useRealtimeConnection(opts) {
-  const { connected, error, reconnect } = useRealtime(opts);
-  return { connected, error, reconnect };
+  const { connected, error, reconnect, reconnectAttempts } = useRealtime(opts);
+  return { connected, error, reconnect, reconnectAttempts };
 }
 var RealtimeContext = react.createContext(null);
 function RealtimeProvider({
diff --git a/dist/react/index.d.cts b/dist/react/index.d.cts
index 4859862..33b504f 100644
--- a/dist/react/index.d.cts
+++ b/dist/react/index.d.cts
@@ -43,6 +43,34 @@ interface UseRealtimeOptions {
     };
     /** socket.io path (WebSocket only). Default `/socket.io`. */
     path?: string;
+    /**
+     * SSE-only: extra named SSE event names to subscribe to, so a server that
+     * emits application-level named events (e.g. `order.created`) can be observed
+     * by this hook. `EventSource` never routes a named event to `onmessage`, so
+     * this hook only sees the reserved/presence catalog by default; listing a
+     * custom name here registers a matching `addEventListener` for it. Ignored on
+     * the WebSocket branch, which already receives every event name via `onAny`.
+     */
+    events?: readonly string[];
+    /**
+     * SSE-only: initial backoff delay in ms before the first automatic
+     * reconnect attempt after an error. Default `1000`. Forwarded to the
+     * internal SSE hook, which already supported it; ignored on WebSocket
+     * (Socket.IO owns its own reconnection policy there).
+     */
+    reconnectInitialMs?: number;
+    /**
+     * SSE-only: maximum backoff delay in ms an automatic reconnect attempt
+     * will wait. Default `30000`. Ignored on WebSocket.
+     */
+    reconnectMaxMs?: number;
+    /**
+     * SSE-only: stop scheduling further automatic reconnect attempts once
+     * this many consecutive failures have been observed. Unset (default)
+     * retries indefinitely. A manual {@link reconnect} call always resets the
+     * counter and resumes retries. Ignored on WebSocket.
+     */
+    maxAttempts?: number;
 }
 /**
  * Universal realtime hook — auto-detects SSE vs WebSocket from the URL scheme.
@@ -70,6 +98,8 @@ declare function useRealtime<TEvents extends Record<string, unknown> = Record<st
     error: Error | undefined;
     emit: (event: string, data: unknown) => void;
     reconnect: () => void;
+    /** Always `0` on the WebSocket branch (Socket.IO owns its own retry policy). */
+    reconnectAttempts: number;
     transport: "websocket";
 } | {
     emit: never;
@@ -78,6 +108,8 @@ declare function useRealtime<TEvents extends Record<string, unknown> = Record<st
     lastEvent: SseEventEntry<TEvents> | undefined;
     error: Error | undefined;
     reconnect: () => void;
+    /** Consecutive failed automatic-reconnect attempts since the last successful open. */
+    reconnectAttempts: number;
     transport: "sse";
 };
 
@@ -99,6 +131,7 @@ declare function useRealtimeConnection(opts: UseRealtimeOptions): {
     connected: boolean;
     error: Error | undefined;
     reconnect: (() => void) | (() => void);
+    reconnectAttempts: number;
 };
 
 /** Return value of {@link usePresence}. */
diff --git a/dist/react/index.d.ts b/dist/react/index.d.ts
index 4859862..33b504f 100644
--- a/dist/react/index.d.ts
+++ b/dist/react/index.d.ts
@@ -43,6 +43,34 @@ interface UseRealtimeOptions {
     };
     /** socket.io path (WebSocket only). Default `/socket.io`. */
     path?: string;
+    /**
+     * SSE-only: extra named SSE event names to subscribe to, so a server that
+     * emits application-level named events (e.g. `order.created`) can be observed
+     * by this hook. `EventSource` never routes a named event to `onmessage`, so
+     * this hook only sees the reserved/presence catalog by default; listing a
+     * custom name here registers a matching `addEventListener` for it. Ignored on
+     * the WebSocket branch, which already receives every event name via `onAny`.
+     */
+    events?: readonly string[];
+    /**
+     * SSE-only: initial backoff delay in ms before the first automatic
+     * reconnect attempt after an error. Default `1000`. Forwarded to the
+     * internal SSE hook, which already supported it; ignored on WebSocket
+     * (Socket.IO owns its own reconnection policy there).
+     */
+    reconnectInitialMs?: number;
+    /**
+     * SSE-only: maximum backoff delay in ms an automatic reconnect attempt
+     * will wait. Default `30000`. Ignored on WebSocket.
+     */
+    reconnectMaxMs?: number;
+    /**
+     * SSE-only: stop scheduling further automatic reconnect attempts once
+     * this many consecutive failures have been observed. Unset (default)
+     * retries indefinitely. A manual {@link reconnect} call always resets the
+     * counter and resumes retries. Ignored on WebSocket.
+     */
+    maxAttempts?: number;
 }
 /**
  * Universal realtime hook — auto-detects SSE vs WebSocket from the URL scheme.
@@ -70,6 +98,8 @@ declare function useRealtime<TEvents extends Record<string, unknown> = Record<st
     error: Error | undefined;
     emit: (event: string, data: unknown) => void;
     reconnect: () => void;
+    /** Always `0` on the WebSocket branch (Socket.IO owns its own retry policy). */
+    reconnectAttempts: number;
     transport: "websocket";
 } | {
     emit: never;
@@ -78,6 +108,8 @@ declare function useRealtime<TEvents extends Record<string, unknown> = Record<st
     lastEvent: SseEventEntry<TEvents> | undefined;
     error: Error | undefined;
     reconnect: () => void;
+    /** Consecutive failed automatic-reconnect attempts since the last successful open. */
+    reconnectAttempts: number;
     transport: "sse";
 };
 
@@ -99,6 +131,7 @@ declare function useRealtimeConnection(opts: UseRealtimeOptions): {
     connected: boolean;
     error: Error | undefined;
     reconnect: (() => void) | (() => void);
+    reconnectAttempts: number;
 };
 
 /** Return value of {@link usePresence}. */
diff --git a/dist/react/index.mjs b/dist/react/index.mjs
index 1b032f9..a574f74 100644
--- a/dist/react/index.mjs
+++ b/dist/react/index.mjs
@@ -26,8 +26,10 @@ function useRealtimeSse(opts) {
   const [events, setEvents] = useState([]);
   const [lastEvent, setLastEvent] = useState(void 0);
   const [error, setError] = useState(void 0);
+  const [reconnectAttempts, setReconnectAttempts] = useState(0);
   const sourceRef = useRef(null);
   const reconnectMsRef = useRef(opts.reconnectInitialMs ?? 1e3);
+  const attemptsRef = useRef(0);
   const timerRef = useRef(null);
   const connect = useCallback(() => {
     if (opts.enabled === false) return;
@@ -42,10 +44,15 @@ function useRealtimeSse(opts) {
       setConnected(true);
       setError(void 0);
       reconnectMsRef.current = opts.reconnectInitialMs ?? 1e3;
+      attemptsRef.current = 0;
+      setReconnectAttempts(0);
     };
     source.onerror = () => {
       setConnected(false);
       setError(new Error("SSE connection error"));
+      attemptsRef.current += 1;
+      setReconnectAttempts(attemptsRef.current);
+      if (opts.maxAttempts !== void 0 && attemptsRef.current >= opts.maxAttempts) return;
       const delay = Math.min(reconnectMsRef.current * 2, opts.reconnectMaxMs ?? 3e4);
       reconnectMsRef.current = delay;
       timerRef.current = setTimeout(connect, delay);
@@ -63,7 +70,12 @@ function useRealtimeSse(opts) {
     for (const name of NAMED_SSE_EVENTS) {
       source.addEventListener(name, handleEvent);
     }
-  }, [opts.enabled, opts.url, opts.withCredentials, opts.reconnectInitialMs, opts.reconnectMaxMs]);
+    if (opts.events) {
+      for (const name of opts.events) {
+        source.addEventListener(name, handleEvent);
+      }
+    }
+  }, [opts.enabled, opts.url, opts.withCredentials, opts.reconnectInitialMs, opts.reconnectMaxMs, opts.maxAttempts, opts.events]);
   useEffect(() => {
     if (opts.enabled === false) return;
     connect();
@@ -78,9 +90,11 @@ function useRealtimeSse(opts) {
   }, [connect, opts.enabled]);
   const reconnect = useCallback(() => {
     reconnectMsRef.current = opts.reconnectInitialMs ?? 1e3;
+    attemptsRef.current = 0;
+    setReconnectAttempts(0);
     connect();
   }, [connect, opts.reconnectInitialMs]);
-  return { connected, events, lastEvent, error, reconnect };
+  return { connected, events, lastEvent, error, reconnect, reconnectAttempts };
 }
 var MAX_EVENTS2 = 100;
 function useRealtimeWs(opts) {
@@ -153,6 +167,10 @@ function useRealtime(opts) {
   const sseResult = useRealtimeSse({
     url: opts.url,
     ...opts.withCredentials !== void 0 ? { withCredentials: opts.withCredentials } : {},
+    ...opts.events !== void 0 ? { events: opts.events } : {},
+    ...opts.reconnectInitialMs !== void 0 ? { reconnectInitialMs: opts.reconnectInitialMs } : {},
+    ...opts.reconnectMaxMs !== void 0 ? { reconnectMaxMs: opts.reconnectMaxMs } : {},
+    ...opts.maxAttempts !== void 0 ? { maxAttempts: opts.maxAttempts } : {},
     enabled: !isWs
   });
   const wsResult = useRealtimeWs({
@@ -162,7 +180,7 @@ function useRealtime(opts) {
     enabled: isWs
   });
   if (isWs) {
-    return { transport: "websocket", ...wsResult };
+    return { transport: "websocket", reconnectAttempts: 0, ...wsResult };
   }
   return {
     transport: "sse",
@@ -174,8 +192,8 @@ function useRealtime(opts) {
 
 // src/react/hooks/use-realtime-connection.ts
 function useRealtimeConnection(opts) {
-  const { connected, error, reconnect } = useRealtime(opts);
-  return { connected, error, reconnect };
+  const { connected, error, reconnect, reconnectAttempts } = useRealtime(opts);
+  return { connected, error, reconnect, reconnectAttempts };
 }
 var RealtimeContext = createContext(null);
 function RealtimeProvider({
diff --git a/dist/server/index.cjs b/dist/server/index.cjs
index 98d4419..2fc080f 100644
--- a/dist/server/index.cjs
+++ b/dist/server/index.cjs
@@ -129,7 +129,8 @@ function createSseController(endpoint) {
     __decorateParam(1, common.Res({ passthrough: true }))
   ], DynamicSseController.prototype, "subscribe", 1);
   DynamicSseController = __decorateClass([
-    common.Controller()
+    common.Controller(),
+    __decorateParam(0, common.Inject(SseSubscriptionHandler))
   ], DynamicSseController);
   return DynamicSseController;
 }
@@ -240,7 +241,8 @@ var RealtimePubSubSubscriber = class {
 RealtimePubSubSubscriber = __decorateClass([
   common.Injectable(),
   __decorateParam(0, common.Inject(REALTIME_PUBSUB_TOKEN)),
-  __decorateParam(1, common.Inject(REALTIME_INSTANCE_ID_TOKEN))
+  __decorateParam(1, common.Inject(REALTIME_INSTANCE_ID_TOKEN)),
+  __decorateParam(2, common.Inject(common.forwardRef(() => SseTransport)))
 ], RealtimePubSubSubscriber);
 var RETRIEVE_LIMIT = 200;
 var OfflineQueueDeliveryService = class {
@@ -1391,7 +1393,7 @@ exports.WebSocketTransport = class WebSocketTransport {
    * @param roomId - The room to join.
    */
   async joinRoom(connectionId, roomId) {
-    const socket = this.server?.sockets.sockets.get(connectionId);
+    const socket = nspSockets(this.server)?.get(connectionId);
     if (socket) {
       await socket.join(roomId);
       this.rooms.join(connectionId, roomId);
@@ -1404,7 +1406,7 @@ exports.WebSocketTransport = class WebSocketTransport {
    * @param roomId - The room to leave.
    */
   async leaveRoom(connectionId, roomId) {
-    const socket = this.server?.sockets.sockets.get(connectionId);
+    const socket = nspSockets(this.server)?.get(connectionId);
     if (socket) {
       await socket.leave(roomId);
       this.rooms.leave(connectionId, roomId);
@@ -1426,7 +1428,7 @@ exports.WebSocketTransport = class WebSocketTransport {
   async disconnect(connectionId, _reason) {
     const server = this.server;
     if (!server) return;
-    const local = server.sockets.sockets.get(connectionId);
+    const local = nspSockets(server)?.get(connectionId);
     if (local) local.disconnect(true);
     server.in(`${CONNECTION_ROOM_PREFIX}:${connectionId}`).disconnectSockets(true);
   }
@@ -1458,6 +1460,10 @@ exports.WebSocketTransport = __decorateClass([
 function firstQueryValue(value) {
   return Array.isArray(value) ? value[0] : value;
 }
+function nspSockets(server) {
+  if (!server) return void 0;
+  return server.sockets instanceof Map ? server.sockets : server.sockets.sockets;
+}
 exports.RealtimeGateway = class RealtimeGateway {
   constructor(transport, options) {
     this.transport = transport;
@@ -2174,6 +2180,14 @@ var RealtimeIoAdapter = class _RealtimeIoAdapter extends platformSocket_io.IoAda
     super(app);
     this.options = app.get(REALTIME_OPTIONS_TOKEN);
   }
+  create(port, options) {
+    const server = super.create(port, options);
+    const ns = this.options.websocket?.namespace;
+    if (ns && ns !== "/" && typeof server?.of === "function") {
+      return server.of(ns);
+    }
+    return server;
+  }
   /**
    * Create the Socket.IO server with merged WebSocket options.
    *
diff --git a/dist/server/index.mjs b/dist/server/index.mjs
index df783ae..a9fc167 100644
--- a/dist/server/index.mjs
+++ b/dist/server/index.mjs
@@ -1,5 +1,5 @@
 import { randomUUID } from 'crypto';
-import { Injectable, Inject, Optional, Logger, Global, Module, Sse, Req, Res, Controller, UnauthorizedException } from '@nestjs/common';
+import { Injectable, Inject, Optional, Logger, Global, Module, Sse, Req, Res, Controller, UnauthorizedException, forwardRef } from '@nestjs/common';
 import { Subject, of, EMPTY, Observable, merge } from 'rxjs';
 import { takeUntil, catchError, finalize } from 'rxjs/operators';
 import { WebSocketServer, WebSocketGateway } from '@nestjs/websockets';
@@ -127,7 +127,8 @@ function createSseController(endpoint) {
     __decorateParam(1, Res({ passthrough: true }))
   ], DynamicSseController.prototype, "subscribe", 1);
   DynamicSseController = __decorateClass([
-    Controller()
+    Controller(),
+    __decorateParam(0, Inject(SseSubscriptionHandler))
   ], DynamicSseController);
   return DynamicSseController;
 }
@@ -238,7 +239,8 @@ var RealtimePubSubSubscriber = class {
 RealtimePubSubSubscriber = __decorateClass([
   Injectable(),
   __decorateParam(0, Inject(REALTIME_PUBSUB_TOKEN)),
-  __decorateParam(1, Inject(REALTIME_INSTANCE_ID_TOKEN))
+  __decorateParam(1, Inject(REALTIME_INSTANCE_ID_TOKEN)),
+  __decorateParam(2, Inject(forwardRef(() => SseTransport)))
 ], RealtimePubSubSubscriber);
 var RETRIEVE_LIMIT = 200;
 var OfflineQueueDeliveryService = class {
@@ -1389,7 +1391,7 @@ var WebSocketTransport = class {
    * @param roomId - The room to join.
    */
   async joinRoom(connectionId, roomId) {
-    const socket = this.server?.sockets.sockets.get(connectionId);
+    const socket = nspSockets(this.server)?.get(connectionId);
     if (socket) {
       await socket.join(roomId);
       this.rooms.join(connectionId, roomId);
@@ -1402,7 +1404,7 @@ var WebSocketTransport = class {
    * @param roomId - The room to leave.
    */
   async leaveRoom(connectionId, roomId) {
-    const socket = this.server?.sockets.sockets.get(connectionId);
+    const socket = nspSockets(this.server)?.get(connectionId);
     if (socket) {
       await socket.leave(roomId);
       this.rooms.leave(connectionId, roomId);
@@ -1424,7 +1426,7 @@ var WebSocketTransport = class {
   async disconnect(connectionId, _reason) {
     const server = this.server;
     if (!server) return;
-    const local = server.sockets.sockets.get(connectionId);
+    const local = nspSockets(server)?.get(connectionId);
     if (local) local.disconnect(true);
     server.in(`${CONNECTION_ROOM_PREFIX}:${connectionId}`).disconnectSockets(true);
   }
@@ -1456,6 +1458,10 @@ WebSocketTransport = __decorateClass([
 function firstQueryValue(value) {
   return Array.isArray(value) ? value[0] : value;
 }
+function nspSockets(server) {
+  if (!server) return void 0;
+  return server.sockets instanceof Map ? server.sockets : server.sockets.sockets;
+}
 var RealtimeGateway = class {
   constructor(transport, options) {
     this.transport = transport;
@@ -2172,6 +2178,14 @@ var RealtimeIoAdapter = class _RealtimeIoAdapter extends IoAdapter {
     super(app);
     this.options = app.get(REALTIME_OPTIONS_TOKEN);
   }
+  create(port, options) {
+    const server = super.create(port, options);
+    const ns = this.options.websocket?.namespace;
+    if (ns && ns !== "/" && typeof server?.of === "function") {
+      return server.of(ns);
+    }
+    return server;
+  }
   /**
    * Create the Socket.IO server with merged WebSocket options.
    *
